# Node / package management
node_modules/
.pnpm-store/

# Build output
dist/
build/
.astro/

# Fetched-at-build artifacts (engine docs, release, discussions) — never hand-copied
src/content/engine-docs/
data/

# Cloudflare tooling
.wrangler/

# Environment — never commit secrets (Discussions read token, API keys)
.env
.env.*

# OS / editor noise
.DS_Store
*.swp
